#include "holberton.h"

/**
 * _strlen - return the length of the string
 * @s: char to check
 * Return: length
 */

int _strlen(char *s)
{
	int a;

	for (a = 0; s[a] != '\0'; a++)
	{
	}

	return (a);
}
